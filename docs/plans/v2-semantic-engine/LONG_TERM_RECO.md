# LONG_TERM_RECO — v2-semantic-engine

One line per item. Remove an item when done and note it in DECISIONS_LOG.md.

- R-001 (D-001, 2026-09-09): raccorder les locators V2, `dry_run` et le rapport structuré aux outils `word_live_*` (COM et JXA) ; when: session sur Windows ou macOS avec Word installé ; est. T3/M
- R-002 (D-001, 2026-09-09): déplacer la branche macOS des outils live à l'intérieur du `try` (erreurs JSON comme sous Windows), ajouter un timeout aux appels COM, unifier les messages « only available on Windows » ; when: même condition que R-001 ; est. T4/S
- R-003 (D-001, 2026-09-09): unifier les 10 outils de notes de bas de page sur le moteur (`ensure_part`, ids, `w:sz` dans `rPr`, `XML_NS` défini avant usage) ; when: après J03 ; est. T3/M — **D-016 (2026-09-10)** : tant qu'ils ne sont pas unifiés, les 5 outils de notes à `paragraph_index` (`add_footnote_to_document`, `add_endnote_to_document`, `add_footnote_enhanced`, `add_footnote_robust_tool`, `add_footnote_to_document_robust`) lisent `doc.paragraphs` (sdt de bloc exclu) et ne composent pas avec l'index rendu par `find_text_in_document` — écart assumé ; l'unification passe par `indexed_paragraphs()`
- R-004 (D-001, 2026-09-09): supprimer `add_restricted_editing`, `add_digital_signature`, `verify_document` et le sidecar `.protection` (protection en nom seulement, signature auto-invalidée) ou les remplacer par `w:documentProtection` ; when: décision produit ; est. T4/S
- ~~R-005~~ **faite le 2026-09-15** (D-037) : nom de fork retenu, la distribution est `word-mcp-semantic`, le dépôt aussi ; publication PyPI non faite, remise à plus tard par le user
- R-006 (D-001, 2026-09-09): `merge_documents` avec notes et commentaires (import des parties avec renumérotation) ; when: besoin réel après J03-P6 ; est. T2/M
- R-007 (D-001, 2026-09-09): création de styles de tableau (formats conditionnels) et outil `normalize_styles` séparé de l'audit ; when: retours d'usage de `doc_audit` ; est. T3/M
- R-008 (D-001, 2026-09-09): enregistrement automatique des 120 wrappers de `main.py` depuis les signatures des implémentations (réconcilier les 104 descriptions divergentes) ; when: après J04 ; est. T4/M
- R-009 (D-001, 2026-09-09, **réduite le 2026-09-15**): image Docker avec LibreOffice pour `convert_to_pdf` ; `docx2pdf` en extra optionnel ; when: besoin réel de `convert_to_pdf` hors poste Windows/macOS ; est. T5/S — le troisième volet (`manifest.json`, `server.json`, `__version__` alignés sur `pyproject.toml`) est **fait** : les quatre déclarations sont à 2.0.0 et `tests/test_version_sync.py` rougit si elles divergent, ce qui était la cause de la dérive
- R-010 (D-001, 2026-09-09): évaluer python-docx ≥ 1.2 (partie commentaires native) et pinner `python-docx>=1.1.2,<2` d'ici là ; when: après J03 ; est. T4/S
- R-011 (D-001, 2026-09-09): `word_live_apply_list` et `word_live_setup_heading_numbering` divergent entre COM et JXA (`outline_numbered` ignoré côté COM, `heading_map` macOS seulement) : réaligner ou documenter la surface macOS ; when: R-001 ; est. T3/M
- R-012 (D-036, 2026-09-15): étendre `accept_tracked_changes`/`reject_tracked_changes` aux genres `w:rPrChange` et `w:pPrChange`, jamais retirés aujourd'hui — mesuré le 2026-09-15 sur les fixtures `tracked_changes` et `combined` (compte inchangé après chacun des deux outils) : accepter toutes les révisions laisse le document porteur de marques que Word affiche encore comme révisions de formatage en attente. Ce n'est pas une régression (J02-P6 refuse explicitement les genres non gérés plutôt que de les ignorer), mais un trou de périmètre du plan. Le travail est propre à chaque genre : un `w:rPrChange` porte l'ancien `rPr` complet, qu'accepter jette et que rejeter doit restaurer sur le run porteur, idem `w:pPrChange` sur le `w:pPr` ; prévoir des fixtures dédiées et des tests d'invariant. Documenté en l'état dans `docs/audit/destructive-ops.md` (notes complémentaires). when: besoin réel d'un aller-retour de révisions complet ; est. T3/M
