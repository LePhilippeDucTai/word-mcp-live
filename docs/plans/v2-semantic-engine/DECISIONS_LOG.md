# DECISIONS_LOG — v2-semantic-engine

Append-only, one line per decision. Never edit past lines.

- 2026-09-09 D-001 Validation du plan: 6 jalons J01→J06 approuvés ; périmètre live limité aux 3 plantages macOS prouvés par lecture (J04-P4) ; outils destructeurs conservés avec avertissement DESTRUCTIVE dès J01-P1 puis réécrits en J03 (reco: idem sur les trois points); long-term → R-001, R-002
- 2026-09-09 D-002 Révision du plan avant exécution: acceptations insatisfaisables ou non déterministes corrigées — J02-P1/P7 `'zipfile' not in sys.modules` (python-docx importe zipfile) → contrôle statique sur `engine/` ; J04-P4/P6 `-W error` sur import (masqué par le `.pyc`) → compilation depuis la source ; J03-P7 dépend de J03-P1..P6 ; `uv.lock` régénéré en commit séparé (périmé depuis v1.1.3, réécrit à chaque `uv run` ; `uv lock --check` stable ensuite) (reco: idem); plan updated: J02-P1, J02-P7, J03-P7, J04-P4, J04-P6
- 2026-09-09 D-003 (orchestrator) Création des worktrees: `isolation: "worktree"` de l'outil Agent branche depuis la branche par défaut (`main`, c6c7617) et ignore `.claude/settings.json` `worktree.baseRef: head` ; conséquence, tout worker part d'une base dépourvue des commits du plan (conflit `uv.lock` au premier merge de J01-P1). Les worktrees de ce plan sont désormais créés à la main (`git worktree add -b worktree-<id> .claude/worktrees/<id> v2-semantic-engine`, dossier gitignoré) et les workers dispatchés sans `isolation`, le chemin de travail donné dans le brief (options: worktree manuel — avancer `main` — résoudre le conflit à chaque merge ; reco: worktree manuel, seul mécanisme qui garantit la base)
- 2026-09-09 D-004 Zones aveugles restantes du harnais J01: nouvelle part J01-P7 (T3) traitant les 3 should-fix de la revue round 2 — `REL-REF-UNRESOLVED` dans `validate_package` (relation `r:id`/`r:embed` d'une story non résolue dans ses `.rels`), `sect_pr` par story dans `snapshot.py` (perte de `headerReference`/`footerReference` aujourd'hui muette), deux tests positifs de `ID-DUPLICATE` sur ids de révision et de commentaire ; dispatchée avant J02-P1. Les 2 findings `optional` (digest du `w:tblGrid`, tests positifs de `XML-MALFORMED` et `CT-PART-MISSING`) restent en note, non traités (options: 3 should-fix — les 5 findings — reporter en J06 ; reco: 3 should-fix, l'instrument porte J02→J06 et une perte de relation ou d'en-tête non vue en J03 coûte plus cher que la part); plan updated: +J01-P7
- 2026-09-09 D-007 Aller-retour `open`/`save` non neutre: `test_package.py:73,116,130` écrivaient `assert difference.is_empty` sans parenthèses (`Diff.is_empty` est une méthode) — trois assertions vides, dont `test_open_then_save_is_lossless` sur 19 fixtures ; corrigées, elles échouent sur 6/6 fixtures mesurées, `_EnginePartFactory` parsant en XML des parties que lxml resérialise sans leur indentation (`word/theme/theme1.xml`, `word/webSettings.xml`, `word/fontTable.xml`, `word/stylesWithEffects.xml`, `docProps/app.xml`, `customXml/itemProps1.xml` ; écart = blancs inter-éléments seuls, aucune donnée perdue). Nouvelle part corrective J02-P8 (T3, S) : corriger les 3 assertions et rendre l'aller-retour neutre en restreignant `_EnginePartFactory` aux parties que le moteur édite, les autres restant en blob (options: part corrective — tolérer la réindentation en la listant en `parts=` — laisser en l'état ; reco: part corrective, c'est le plancher que le jalon revendique et J03 migrera 120 outils par-dessus cet aller-retour ; tolérer rendrait sourd tout `assert_unchanged_except` sur ces 6 parties pour J03→J06); plan updated: +J02-P8
