# DECISIONS_LOG — v2-semantic-engine

Append-only, one line per decision. Never edit past lines.

- 2026-09-09 D-001 Validation du plan: 6 jalons J01→J06 approuvés ; périmètre live limité aux 3 plantages macOS prouvés par lecture (J04-P4) ; outils destructeurs conservés avec avertissement DESTRUCTIVE dès J01-P1 puis réécrits en J03 (reco: idem sur les trois points); long-term → R-001, R-002
- 2026-09-09 D-002 Révision du plan avant exécution: acceptations insatisfaisables ou non déterministes corrigées — J02-P1/P7 `'zipfile' not in sys.modules` (python-docx importe zipfile) → contrôle statique sur `engine/` ; J04-P4/P6 `-W error` sur import (masqué par le `.pyc`) → compilation depuis la source ; J03-P7 dépend de J03-P1..P6 ; `uv.lock` régénéré en commit séparé (périmé depuis v1.1.3, réécrit à chaque `uv run` ; `uv lock --check` stable ensuite) (reco: idem); plan updated: J02-P1, J02-P7, J03-P7, J04-P4, J04-P6
- 2026-09-09 D-003 (orchestrator) Création des worktrees: `isolation: "worktree"` de l'outil Agent branche depuis la branche par défaut (`main`, c6c7617) et ignore `.claude/settings.json` `worktree.baseRef: head` ; conséquence, tout worker part d'une base dépourvue des commits du plan (conflit `uv.lock` au premier merge de J01-P1). Les worktrees de ce plan sont désormais créés à la main (`git worktree add -b worktree-<id> .claude/worktrees/<id> v2-semantic-engine`, dossier gitignoré) et les workers dispatchés sans `isolation`, le chemin de travail donné dans le brief (options: worktree manuel — avancer `main` — résoudre le conflit à chaque merge ; reco: worktree manuel, seul mécanisme qui garantit la base)
- 2026-09-09 D-004 Zones aveugles restantes du harnais J01: nouvelle part J01-P7 (T3) traitant les 3 should-fix de la revue round 2 — `REL-REF-UNRESOLVED` dans `validate_package` (relation `r:id`/`r:embed` d'une story non résolue dans ses `.rels`), `sect_pr` par story dans `snapshot.py` (perte de `headerReference`/`footerReference` aujourd'hui muette), deux tests positifs de `ID-DUPLICATE` sur ids de révision et de commentaire ; dispatchée avant J02-P1. Les 2 findings `optional` (digest du `w:tblGrid`, tests positifs de `XML-MALFORMED` et `CT-PART-MISSING`) restent en note, non traités (options: 3 should-fix — les 5 findings — reporter en J06 ; reco: 3 should-fix, l'instrument porte J02→J06 et une perte de relation ou d'en-tête non vue en J03 coûte plus cher que la part); plan updated: +J01-P7
- 2026-09-09 D-007 Aller-retour `open`/`save` non neutre: `test_package.py:73,116,130` écrivaient `assert difference.is_empty` sans parenthèses (`Diff.is_empty` est une méthode) — trois assertions vides, dont `test_open_then_save_is_lossless` sur 19 fixtures ; corrigées, elles échouent sur 6/6 fixtures mesurées, `_EnginePartFactory` parsant en XML des parties que lxml resérialise sans leur indentation (`word/theme/theme1.xml`, `word/webSettings.xml`, `word/fontTable.xml`, `word/stylesWithEffects.xml`, `docProps/app.xml`, `customXml/itemProps1.xml` ; écart = blancs inter-éléments seuls, aucune donnée perdue). Nouvelle part corrective J02-P8 (T3, S) : corriger les 3 assertions et rendre l'aller-retour neutre en restreignant `_EnginePartFactory` aux parties que le moteur édite, les autres restant en blob (options: part corrective — tolérer la réindentation en la listant en `parts=` — laisser en l'état ; reco: part corrective, c'est le plancher que le jalon revendique et J03 migrera 120 outils par-dessus cet aller-retour ; tolérer rendrait sourd tout `assert_unchanged_except` sur ces 6 parties pour J03→J06); plan updated: +J02-P8
- 2026-09-09 D-008 Marque de paragraphe insérée dans accept/reject: `accept`/`reject` de `engine/revisions.py` refusent le genre `paragraph-mark-ins`, la lettre du scope J02-P6 ne couvrant que la marque supprimée ; conséquence, `accept(pkg)` sans filtre lève `UnsupportedRevision` en bloc sur tout document où un relecteur a ajouté un paragraphe. Nouvelle part de suivi J02-P9 : accepter = retirer la marque, rejeter = fusionner avec le paragraphe suivant, en réutilisant `_merge_into_next` déjà écrite et testée (options: part de suivi — élargir le scope de J03-P2 — limite connue documentée ; reco: part de suivi, un `accept` global qui échoue sur un document ordinaire rend l'outil migré en J03-P2 inutilisable et le mécanisme existe déjà); plan updated: +J02-P9
- 2026-09-09 D-006 Vocabulaire des stories et forme de Match: entériner les choix de J02-P5 — `"body"` reste un alias exclusif de `"document"` en entrée de `find`, `Match.story` rapportant toujours l'id réel de `DocxPackage.stories()` ; `Match.paragraph` est l'élément `w:p` vivant et `Match.index` l'entier V2 séparé, `None` pour un paragraphe en cellule de tableau (hors de l'espace d'index V2) ; le vocabulaire des jalons J03 et J04 est aligné dessus (options: entériner — supprimer l'alias et n'exposer que les ids de stories() ; reco: entériner, l'alias est documenté et sans ambiguïté et `int | None` dit une vérité de l'adressage V2); plan updated: J03, J04 (vocabulaire)
- 2026-09-09 D-009 Should-fix de la revue J02: nouvelle part corrective J02-P10 (T3, S) avant J03 pour les deux défauts prouvés par sonde en J02-P7 — (1) `engine/revisions.py:603`, `_free_of_insertions` passe à `_split_around` un `child` qui n'est pas enfant direct du `w:ins` englobant, donc `tracked_insert` sort un run de son `w:hyperlink`/`w:sdt`/`w:smartTag` et le lien est perdu ; (2) `engine/find.py:141`, `_v2_index_map` ne filtre que `w:tc` alors qu'`iter_paragraphs` descend dans `w:txbxContent`/`mc:AlternateContent`, donc un paragraphe de zone de texte consomme un index V2 et décale tous les suivants ; le finding `optional` (réexport de `LIVE_CONTENT_TYPES` par `engine/__init__.py`) est embarqué. Aucune des 19 fixtures ne couvre ces formes : la part ajoute les fixtures qui les prouvent (options: part corrective J02-P10 — élargir J03-P1 et J03-P2 — limites documentées ; reco: part corrective, l'espace d'index V2 est le contrat que J03 et J04 consomment 120 fois, un index faux ne se voit pas en test et se paie en édition du mauvais paragraphe); plan updated: +J02-P10, J03-P1, J03-P3, J03-P4, J03-P8, J04-P1, J04-P2
- 2026-09-09 D-005 Lecture du résultat en cache d'un champ imbriqué: garder la lecture mécanique de J02-P2 — le résultat en cache d'un champ imbriqué (un `PAGE` dans l'instruction d'un `IF`) est stocké en `w:t` ordinaire et reste donc du texte visible pour `textmodel`, `fields()` signalant les deux spans pour que `ranges` et `find` refusent d'y couper ; `fields()` peut rendre `"1first page"` là où Word afficherait `"first page"`, écart assumé et documenté (options: lecture mécanique — masquer le résultat des champs imbriqués ; reco: lecture mécanique, le moteur rapporte ce que le paquet stocke, masquer reviendrait à simuler le moteur de champs de Word et rouvrirait J02-P2/P3/P5); plan updated: Risks (écart documenté)
- 2026-09-09 D-010 (orchestrator) Signature de `find_and_replace_text` après migration: garder la signature stricte `DocxPackage` (un `Document` python-docx lève un `TypeError` explicite) — c'est un helper interne de `utils/document_utils.py`, son seul appelant `content_tools.search_and_replace` est migré, et l'outil MCP `search_and_replace` garde nom, paramètres, message et format de retour, donc l'invariant « aucune rupture d'API » du plan tient (options: signature stricte — accepter aussi un `Document` en le réouvrant depuis le disque ; reco: signature stricte, un `Document` python-docx ne donne pas d'accès vivant aux parties footnotes/endnotes et rendrait `stories()` partiel, donc un remplacement silencieusement incomplet)
- 2026-09-09 D-011 (orchestrator) CHANGELOG.md pour le changement de base de merge_documents: laisser J03-P7/J03-P8 documenter en un seul passage le changement de comportement de `merge_documents` (le document fusionné est désormais le premier source, plus un `Document()` vierge) et le refus des sources à commentaires/notes — `CHANGELOG.md` n'est dans les `files` d'aucune part J03 encore en vol et un ajout ici aurait risqué un conflit avec J03-P1/P3 (options: documenter maintenant hors files — laisser J03-P7/P8 ; reco: laisser J03-P7/P8, le fichier est exposé à plusieurs parts parallèles et la revue J03-P8 est le point naturel pour un CHANGELOG cohérent sur tout le jalon)
- 2026-09-09 D-012 (orchestrator) Réparation des 5 rouges laissés par J03-P2: part corrective dédiée avant J03-P5 plutôt que d'attendre J03-P7 ou d'élargir J03-P5 — diagnostiqué en séance : `test_track_replace_infinite_loop_when_replacement_contains_original` et les 2 xfail de non-fusion de marque de paragraphe passent désormais (XPASS, le bug est corrigé), les 2 tests `touches_only_the_paragraphs_with_ins_or_del` ont des listes de paragraphes attendues périmées par la fusion réelle (D-008/J02-P9) ; confirmé par le user en séance (options: part corrective maintenant — J03-P7 — élargir J03-P5 ; reco: part corrective maintenant, la base reste verte pour le reste de la vague J03); plan updated: +J03-P9, J03-P5, J03-P7, J03-P8
- 2026-09-09 D-013 Contention LibreOffice dans le harnais: nouvelle part corrective J03-P10 (T5, S) sur `tests/support/libreoffice.py` seul — passer `-env:UserInstallation=file://<profil temporaire unique>` à chaque invocation de `soffice` dans `convert()`, car `soffice` s'attache sinon à l'instance déjà lancée qui détient `~/.config/libreoffice` et sort en code 0 sans écrire le fichier de sortie (`RuntimeError: soffice reported success but did not produce <out_path>`, `tests/support/libreoffice.py:44-80`) ; mesuré en s4 au merge de J03-P1, trois exécutions successives de la même acceptation sur la base : 1 échec, 2 échecs sur d'autres tests, puis 72 passed, victimes différentes, aucune régression de code. Portée : `tests/characterization` figure dans les acceptations de J03-P5, J03-P7, J03-P8, J03-P9 et J06-P3 s'appuie aussi sur LibreOffice (options: part corrective maintenant — élargir les `files` de J03-P7 — sérialiser à la main ; reco: part corrective, l'instrument porte J03→J06 et un faux rouge aléatoire rend un vrai rouge indistinguable à chaque merge de vague); plan updated: +J03-P10
- 2026-09-10 D-014 (user) Point d'intégration sur `main`: `main` reste intact (66 commits d'avance sur la branche, 0 de retard) jusqu'à la revue J03-P8 — cette session pousse `v2-semantic-engine` sur origin et s'arrête là, la session suivante lance la revue puis merge sur main. Motif : J03 réécrit la surface des outils existants (120 noms, signatures et formats de retour censés être inchangés) et J03-P8 est le seul passage qui relit le jalon de bout en bout, J02-P10 comprise ; un finding bloquant découvert après un merge se corrigerait sur main (options: après la revue J03-P8 — maintenant, J03 non relue ; reco: après la revue, tranché par le user en séance)
- 2026-09-10 D-015 CHANGELOG des changements de comportement de J03: nouvelle part J03-P11 (T5, S) sur `CHANGELOG.md` seul, dispatchable en parallèle de la revue J03-P8, plutôt que d'attendre J04 ou de renoncer — D-011 confiait la rédaction à « J03-P7 ou J03-P8 » mais `CHANGELOG.md` n'est dans les `files` d'aucune part et J03-P8 est une revue (elle ne change rien). Trois changements à écrire : `merge_documents` fusionne désormais dans le premier source (plus un `Document()` vierge, ce qui préserve styles, sections et en-têtes) et refuse explicitement une source contenant commentaires ou notes (J03-P6) ; un `end_anchor_text` introuvable est signalé (`"End anchor 'X' not found."`) au lieu de supprimer silencieusement jusqu'à la fin du document (J03-P5) ; un bloc portant plus de sauts de section qu'il ne reste de paragraphes porteurs est refusé au lieu d'en perdre un (J03-P5) (options: J03-P11 avant la revue — reporter en J04 — renoncer ; reco: J03-P11, un changement de comportement non écrit est une rupture d'API silencieuse pour l'appelant et `main` hérite de J03 dès la revue verte par D-014); plan updated: +J03-P11
- 2026-09-10 D-016 Espace d'index des points d'appel restés sur `doc.paragraphs`: part corrective en tête de J04 qui aligne `get_paragraph_text` (`utils/extended_document_utils.py:63`), `set_paragraph_spacing` (`tools/layout_tools.py:351`) et le `target_paragraph_index` des 3 `insert_*_near_text` (`utils/document_utils.py:422,471,565`) sur `indexed_paragraphs()`, **sans** les 5 outils de notes de bas de page (`tools/footnote_tools.py:58,129,319,465,625`), que le PLAN.md met hors périmètre et que R-003 prévoit déjà d'unifier sur le moteur — J03-P1 a fait passer `find_text_in_document` à l'index V2 et J03-P5 a suivi pour `delete_paragraph`/`add_bookmark`, les autres consommateurs d'un `paragraph_index` public sont restés sur `doc.paragraphs` : mesuré par la revue J03-P8 round 2 sur un document à sommaire, `find_text_in_document("Charlie") → 5` puis `add_footnote_to_document(5, "note")` pose la note sur « Echo » en répondant `Footnote added to paragraph 5`, et `get_paragraph_text(5) → "Echo"` ; aucun n'est destructeur mais tous rendent un résultat faux dès qu'un `w:sdt` de bloc est présent, et `add_table_of_contents` en insère un (options: corrective sans les notes — corrective incluant les notes — acter la divergence et l'écrire au CHANGELOG ; reco: corrective sans les notes, l'espace d'index est le contrat que J04-P1 va réutiliser et le laisser à deux valeurs sous le même nom de paramètre garantit qu'un appelant compose deux outils et corrompt son document, tandis que les notes sont hors scope du plan et déjà couvertes par R-003); plan updated: J04-P7, J04-P1, J04-P6
- 2026-09-10 D-017 (orchestrator) 5e puce `[Unreleased]` sur le changement d'espace d'index: écrire la puce dans la part corrective de D-016 plutôt qu'en part `CHANGELOG.md` dédiée ou pas du tout — les 4 puces existantes couvrent `merge_documents`, `add_header_footer` et 2 garde-fous, mais pas le changement le plus visible pour l'appelant : `paragraph_index` de `find_text_in_document`/`delete_paragraph`/`add_bookmark` compte désormais les paragraphes d'un `w:sdt` de bloc, ce qui invalide les index qu'un appelant avait enregistrés. Tranchée sans question : D-015 est déjà l'énoncé du principe (« un changement de comportement non écrit est une rupture d'API silencieuse pour l'appelant ») et D-016 fixe le périmètre exact que la puce doit décrire, donc son contenu final ; local et réversible (options: puce dans la part corrective de D-016 — part CHANGELOG dédiée — ne rien écrire ; reco: puce dans la part corrective de D-016); plan updated: J04-P7
- 2026-09-10 D-018 (orchestrator) `add_bookmark` sans borne inférieure: ajouter `paragraph_index < 0 or` à la garde de `tools/layout_tools.py:472` dans la part corrective de D-016 — la garde ne teste que `paragraph_index >= len(paragraphs)`, donc `add_bookmark(path, -1, "Neg")` signe le dernier paragraphe et répond `{"success": true, "paragraph_index": -1}` là où `delete_paragraph(-1)` refuse avec `Invalid paragraph index. Document has 3 paragraphs (0-2).`. Défaut antérieur au jalon, mais J03 vient de mettre les deux outils dans le même espace d'index : leurs bornes divergentes sont devenues un piège d'appelant neuf. Tranchée sans question : une ligne, local et réversible, et l'invariant de compatibilité du PLAN.md porte sur les noms, paramètres et formats de retour, pas sur l'acceptation d'une entrée invalide (options: ajouter la garde — laisser tel quel ; reco: ajouter la garde); plan updated: J04-P7
- 2026-09-10 D-019 Ponctuation doublée de `search_and_replace`: corriger le séparateur pour le cas « 0 remplacement » dans la part corrective de D-016, test `tests/tools/test_search_replace.py:194` mis à jour — quand toutes les occurrences tombent dans un champ, `tools/content_tools.py:647` rend `No occurrences of 'X' found., 1 skipped (inside fields)`. L'invariant du PLAN.md (formats de retour des 120 outils inchangés) protège ce qu'un appelant peut parser ; cette virgule n'est décrite nulle part et le suffixe `, N skipped (inside fields)` a été ajouté par J03-P1, donc la forme fautive est neuve et non héritée (options: corriger le séparateur — laisser tel quel ; reco: corriger le séparateur); plan updated: J04-P7
- 2026-09-15 D-024 (user) 2 should-fix de la revue J04-P6: nouvelle part corrective en tête de J05 (T4, S) plutôt que laisser en l'état — (1) `capabilities.py` n'exporte pas de `TOOLS`, `doc_capabilities` n'est jamais enregistré (125 outils, aucun `doc_capabilities` mesuré) alors que PLAN.md/DESIGN-REVIEW.md le listent comme livrable J04 ; (2) `doc_apply_edits` (`tools/v2/batch.py:91-97`) lit chaque charge par `.get()` sans valider le jeu de clés, une faute de frappe (`content` au lieu de `text`) devient un `replace` avec `text=""`, efface le span visé et sauvegarde en répondant `status: ok` — prouvé par exécution, `doc_edit_text` refuse le même typo sans toucher au fichier (options: part corrective dédiée — laisser en l'état ; reco: part corrective, tranché par le user en séance); plan updated: +J05-P0, J05-P6
- 2026-09-15 D-020 (user) Point d'intégration sur `main`: merger `v2-semantic-engine` sur `main` seulement après J04-P7, pas maintenant malgré D-014 remplie à la lettre (revue J03-P8 verte) — un merge intermédiaire livrerait sur `main` deux sens de `paragraph_index` sous le même nom de paramètre tant que J04-P7 n'a pas aligné les 5 points d'appel restants sur l'espace d'index V2 (options: après J04-P7 — maintenant — pull request depuis la branche ; reco: après J04-P7, tranché par le user en séance)
- 2026-09-15 D-021 (user) Message non déterministe d'`insert_line_or_paragraph_near_text`: corriger dans une part J05 plutôt que laisser tel quel — `utils/document_utils.py` n'est dans les `files` d'aucune part J04 ouverte, mais J05-P4 (numérotation) le touche déjà dans la même zone (`insert_numbered_list_near_text`, `:406-525`) ; le fold dans le scope de J05-P4 plutôt qu'une part T5 dédiée (options: part J05 — laisser tel quel ; reco: part J05, tranché par le user en séance); plan updated: J05-P4, J05-P6
- 2026-09-15 D-023 (orchestrator) Classification `PLATFORMS` des outils live: garder la classification de J04-P3 issue de la lecture de chaque branche `_MAC_AVAILABLE` (appel `mac_*` réel vs stub d'erreur vs aucune branche) — 31 outils macOS-capables / 14 Windows-only sur les 45 outils live, plutôt que l'estimation approximative du brief (« 30 macOS, 15 Windows ») ; l'écart tient à `word_screen_capture`, qui a une implémentation `mac_screen_capture` réelle malgré un docstring « [Windows only] » périmé (l'étiquette est fausse sur tous les outils live, signal non fiable). Aucune table d'audit source dans le dépôt pour trancher autrement (options: classification par le code — forcer le compte du brief ; reco: classification par le code, c'est la seule source vérifiable); plan updated: aucun (précision d'exécution, pas un changement de scope)
- 2026-09-15 D-022 (orchestrator) Codes d'erreur des locators V2 (`engine/locators.py`, `LocatorError`): garder `not_found`/`ambiguous`/`stale_anchor` (souligné) plutôt que `not-found`/`out-of-range` (tiret) utilisés par le reste du moteur — J04-P7 surfacée par le worker de J04-P1 ; PLAN.md et DESIGN-REVIEW.md écrivent déjà `stale_anchor`/`ambiguous` soulignés pour la surface V2, l'écart avec les codes hérités du moteur (tiret) est assumé et documenté dans le docstring de `locators.py` (options: souligné (surface V2) — tiret (aligner sur le moteur) ; reco: souligné, c'est la graphie du plan et de la revue de conception)
- 2026-09-15 D-025 (user) Réécriture complète du README: le README doit être entièrement réécrit en fin de session pour décrire le MCP tel qu'il est réellement implémenté (cœur OOXML `engine/`, surface V2 `doc_*` sans état, outils existants migrés, outils live COM/JXA, harnais de fidélité), et non plus le texte hérité de l'amont — nouvelle part J05-P7 (T3, M, `README.md` seul) plutôt qu'une retouche à la main par l'orchestrateur, parce que le README est aussi une sortie générée : le bloc entre `<!-- tool-counts:start -->` et `<!-- tool-counts:end -->` et le trailer `<!-- mcp-name: … -->` sont produits par `scripts/gen_tools_md.py` et gardés par `tests/test_docs_sync.py`, donc une réécriture qui les perd casse `gen_tools_md.py --check`. `README.md` est déjà dans les `files` de J05-P0 (régénération des compteurs à 126 outils) : J05-P7 en dépend pour ne jamais tourner dans la même vague (options: part dédiée après J05-P0 — retouche orchestrateur en clôture ; reco: part dédiée, tranché par le user en séance); plan updated: +J05-P7
- 2026-09-15 D-026 (user) Bascule de la base sur `main`: à la clôture de cette session, `v2-semantic-engine` est fusionnée dans `main` (94 commits d'avance, 0 de retard — fusion sans divergence) et **toutes les sessions suivantes travaillent sur `main`**, qui devient la `Base branch` du PLAN.md ; D-014 puis D-020 avaient reporté ce point d'intégration jusqu'à J04-P7, qui est ✅ depuis s7 avec la revue J04-P6 sans finding bloquant, donc la condition qu'elles posaient est remplie. Les worktrees des vagues suivantes redeviennent des worktrees `isolation: "worktree"` du harnais, la base étant alors la branche par défaut du dépôt (fin de l'exception D-003) (options: fusionner en fin de s8 et basculer la base — rester sur la branche jusqu'à la fin de J06 — passer par une pull request ; reco: fusionner, tranché par le user en séance); plan updated: PLAN.md Base branch, Conventions
- 2026-09-15 D-028 (orchestrator) Tolérance de `apply_tint_shade` (`engine/theme.py`, J05-P1): garder la réimplémentation HSL avec une tolérance de ≤ 2 par canal et le statut de **hint de rendu, jamais réécrit dans le document**, plutôt que d'ajuster des constantes jusqu'à reproduire les échantillons de Word — la fixture cache la sortie de Word pour `accent1 + themeTint="66"` → `B4C6E7` et `+ themeShade="BF"` → `2F5597`, l'implémentation rend `B4C7E7` et `2F5496`, soit un cran d'écart sur un ou deux canaux ; le worker a essayé RGB linéaire, plusieurs quantifications HSL (échelles 255 et 240) et trois règles d'arrondi sans qu'aucune ne reproduise les deux paires, l'arithmétique de Word étant en virgule fixe. Le test épingle l'algèbre exacte (`FF` identité, `00` saturation, un `tint` n'assombrit jamais) et borne l'erreur, il ne cale pas des constantes sur deux échantillons. Tranchée sans question : le PLAN.md pratique déjà l'écart assumé et documenté (D-005 champs imbriqués, D-009 conteneur vidé, styles de tableau non résolus de J05-P2), aucune valeur n'est écrite dans le paquet donc aucun invariant de fidélité n'est en jeu, et c'est local et réversible. J05-P2 (format effectif, couleurs par thème + tint/shade) hérite de la même tolérance et ne doit pas la redécouvrir comme un bug ; la revue J05-P6 ne la compte pas comme finding (options: tolérance documentée — rétro-ingénierie de l'arithmétique de Word — ne pas résoudre tint/shade ; reco: tolérance documentée)
- 2026-09-15 D-029 (orchestrator) Locator d'un usage de style porté par un tableau ou une zone de texte (`find_style_usage`, J05-P1): rendre une adresse partielle `{"story", "table"}` pour un tableau et `locator: None` pour un paragraphe de zone de texte, plutôt qu'un locator complet fabriqué — le vocabulaire de locators (PLAN.md, Adressage V2) n'a aucune forme désignant un tableau entier, et émettre `{"table": t, "row": 0, "col": 0}` rendrait une adresse qui **résout**, vers un paragraphe arbitraire qu'un agent pourrait ensuite formater par erreur ; l'adresse partielle échoue en `invalid`, bruyamment. Cohérent avec D-006/D-009, qui posent déjà `Match.index = None` pour un paragraphe sans index V2. Tranchée sans question : couverte par le PLAN.md, locale et réversible (options: adresse partielle — locator complet fabriqué — omettre ces usages ; reco: adresse partielle)
- 2026-09-15 D-030 (orchestrator) Contradiction du Scope de J05-P3 sur `create_custom_style`: garder la signature de l'outil **strictement intacte** et porter le paramètre `style_type` sur `core.styles.create_style` (implémentation), comme le worker l'a fait, plutôt que d'ajouter un paramètre à l'outil MCP — le Scope de J05-P3 écrivait à la fois « garde sa signature » et « accepte `style_type` `paragraph` ou `character` », or l'outil n'a pas de `style_type` et l'ajouter imposerait de toucher `main.py` et `TOOLS.md`, absents des `files` de la part. Tranchée sans question : l'invariant de compatibilité du PLAN.md (noms, paramètres et formats de retour inchangés, nouveaux paramètres optionnels seulement) est tenu à la lettre par ce choix, il est local et réversible, et exposer `style_type` à l'appelant MCP reste possible plus tard sans rien casser (options: signature intacte, `style_type` côté implémentation — nouveau paramètre optionnel sur l'outil, `main.py` et TOOLS.md élargis ; reco: signature intacte, c'est la lecture stricte de l'invariant et la part n'a pas les fichiers pour l'autre)
- 2026-09-15 D-027 (user) Documentation de dépôt à jour pour J05: nouvelle part T5 dédiée en fin de J05, `CHANGELOG.md` et `CONTRIBUTING.md` seuls, dispatchable en parallèle de J06 — plutôt que de reporter à J06-P3 ou de ne rien écrire. Sept changements de comportement visibles par un appelant, tous confirmés dans le diff par la revue J05-P6, n'ont de part propriétaire nulle part (même situation que D-015 pour J03, qui avait créé J03-P11) : `insert_numbered_list_near_text` alloue une définition de liste neuve par appel au lieu des `numId` 1/2 en dur (deux appels successifs ne numérotent plus en continu) et n'applique plus le repli sur `Normal` quand `List Paragraph` manque ; `insert_line_or_paragraph_near_text` répond `with style 'Normal'.` au lieu du `repr` d'un objet Python variable d'une exécution à l'autre (D-021) ; `core.styles.ensure_heading_style` et `core.styles.create_style` écrivent réellement au lieu d'être des no-op, cette dernière rendant un `StyleInfo` au lieu d'un `Style` python-docx ; `create_custom_style` écrit vraiment le style et refuse une couleur illisible ; `add_heading` et `create_document` peuvent désormais écrire `word/styles.xml` (jusqu'à 9 styles de titre créés) ; `add_heading` peut désormais échouer sur un document sans style `Normal` (objet de D-032, dont le CHANGELOG porte la trace quelle que soit l'issue). Second point de la même famille : `CONTRIBUTING.md` demande « Update the tool count in README.md badges and text » alors que ce bloc est généré par `scripts/gen_tools_md.py` depuis J04-P3 et gardé par `tests/test_docs_sync.py` — suivre l'instruction casse `gen_tools_md.py --check` (relevé par le worker de J05-P7) (options: part T5 dédiée en fin de J05 — reporter à J06-P3 — ne rien écrire ; reco: part dédiée, un changement de comportement non écrit est une rupture d'API silencieuse et `main` hérite de J05 dès la clôture de s8 par D-026, tranché par le user en séance); plan updated: +J05-P8, J06-P3, J06-P4
- 2026-09-15 D-032 (user) Findings de la revue J05-P6: part corrective couvrant les deux findings, plutôt que le should-fix seul ou rien. (1) **should-fix**, `core/styles.py:106` — `ensure_heading_style` ne rattrape que `already_exists` ; sur un `.docx` dont la feuille ne définit pas de style `Normal` et à qui il manque des `HeadingN`, `_reference_id` lève `LocatorError("not_found")`, qui traverse `ensure_heading_style` et fait répondre à l'outil historique `add_heading` « Failed to add heading: 'based_on' names 'Normal', which this document does not define… », là où l'ancien code (`except Exception: pass`, « we'll just use default formatting ») insérait le titre ; reproduit par exécution par la revue ; un paquet sans partie `styles` échoue de même par `PackageError`, jamais rattrapée. Correctif le plus petit : élargir le `except` à `EngineError`, ou omettre `based_on`/`next` quand `Normal` est absent — « ensure » est au mieux-effort par contrat. (2) **optional**, `engine/effective.py:194` — la constante `_OFF` est recopiée de `engine/styles.py:246` au lieu d'être importée : aucune incohérence aujourd'hui (les jeux de clés des deux décodeurs sont disjoints et la sémantique trois-états est identique), mais élargir le vocabulaire de bascule d'un seul côté ferait diverger les six propriétés extras des cinq du modèle ; correctif : importer `_OFF` ou exposer un `toggle_value()` public dans `styles.py` (options: part corrective couvrant les deux — corriger le should-fix seul — laisser en l'état et l'écrire au CHANGELOG ; reco: part corrective couvrant les deux, la régression casse un outil historique sur un document réel et l'invariant de compatibilité du PLAN.md est précisément ce qui est en jeu, tranché par le user en séance); plan updated: +J05-P9, J06-P4
- 2026-09-15 D-033 (user) `docs/audit/destructive-ops.md` périmé: le réécrire en inventaire « état avant le plan → état après », avec le renvoi vers le test de caractérisation qui gèle chaque correctif, plutôt que le retirer du dépôt ou le coiffer d'un avertissement. Le document, produit par J01-P5 comme inventaire de départ, décrit comme cassés 8 outils/comportements tous corrigés depuis et dont plus aucun n'est `xfail` dans `tests/characterization/test_existing_tools.py` (`format_text`, `add_table_of_contents`, `merge_documents`, `add_header_footer`, `create_custom_style`, `replace_block_between_manual_anchors`, `track_replace`, la fusion de marque de paragraphe d'`accept`/`reject_tracked_changes`) ; il n'est dans les `files` d'aucune part, J06-P3 l'a signalé et laissé intact comme demandé (options: réécrire en avant/après — retirer du dépôt, l'inventaire vivant étant `tests/characterization` — avertissement en tête, contenu inchangé ; reco: réécrire en avant/après, c'est la seule trace de ce que le plan a réparé, le retirer perdrait la photo de l'état initial et le laisser faux ferait retomber une session future dans le piège); plan updated: +J06-P5
- 2026-09-15 D-034 (user) Docstrings d'audit à 12 contrôles contre 15 genres: corriger les deux docstrings (`engine/audit.py:1147`, `tools/v2/audit.py:44`) en « fifteen finding kinds across twelve checks », **embarqué dans la part corrective** ouverte par D-033/D-035 plutôt qu'en part dédiée ou sans suite. Les 12 contrôles du Scope de J06-P1 se subdivisent en 15 genres (signets et plages de commentaire donnent chacun un `without_start` et un `without_end`) ; `len(FINDING_KINDS) == 15` vérifié par exécution le 2026-09-15, et le docstring dit littéralement « in the order of FINDING_KINDS », donc l'écart est un signal faux pour qui ajoute un genre ; J06-P3 avait évité de citer un compte dans le README pour ne pas le propager (options: corriger embarqué dans une corrective — part dédiée T6 — ne rien faire ; reco: corriger embarqué, deux lignes ne valent pas une part et l'écart induit en erreur); plan updated: +J06-P5
- 2026-09-15 D-035 (user) Findings de la revue J06-P4: part corrective T4 couvrant **les 4** (les 3 should-fix et l'optional), plutôt que les should-fix seuls, les deux points de fidélité seuls, ou un renvoi en LONG_TERM_RECO. Tous reproduits par exécution, aucun bloquant : (1) `tools/v2/compare.py:176` — `engine_snapshot` lit le zip avant tout `DocxPackage.open` (`:180`) et `zipfile.BadZipFile` n'est ni dans `_REPORTED` ni une `OSError`, donc un `.docx` tronqué ou renommé fait lever hors de l'outil là où `doc_inspect` et `doc_audit` répondent `{"status":"error","code":"package_error"}` — l'appelant ne peut pas brancher sur `code`, et le docstring de `tools/v2/registry.py` promet le contraire ; correctif : ouvrir `DocxPackage.open` sur les deux fichiers avant les `engine_snapshot`, ou mapper `BadZipFile` en `PackageError` ; (2) `tests/e2e/test_agent_flows.py:195` — la garde nomme `word/theme1.xml`, qui n'est pas une partie du paquet (`word/theme/theme1.xml` l'est) : ce tiers de l'assertion est inerte et une réécriture du thème à la sauvegarde, exactement la classe de régression D-007, la traverserait ; (3) `engine/audit.py:931` — `_referenced_styles` ne balaie que `pkg.stories()`, commentaires exclus : un style personnalisé utilisé seulement dans `word/comments.xml` est rapporté `unused_custom_style` et un agent qui agit sur le finding le supprime ; correctif : ajouter la racine des commentaires au balayage des `w:pStyle`/`w:rStyle` ; (4) optional, la garde grep de D-007 (`jalons/J02-coeur-ooxml.md:170`) ne balaie que `tests/` alors que `Diff` et un porteur (`delta.is_empty()`, `tools/v2/compare.py:184`) vivent désormais sous `word_document_server/` depuis le déplacement de J06-P2. La part porte aussi D-033 (réécriture de `docs/audit/destructive-ops.md`) et D-034 (docstrings d'audit) (options: les 4 — les 3 should-fix seulement — les deux points de fidélité seulement — aucun, tout en LONG_TERM_RECO ; reco: les 4, chaque correctif tient en quelques lignes et les deux gardes inertes rendent le harnais sourd à la régression que tout le plan s'est employé à empêcher); plan updated: +J06-P5
- 2026-09-15 D-036 (user) `w:rPrChange`/`w:pPrChange` jamais retirés par `accept`/`reject`: ligne LONG_TERM_RECO datée (R-012) plutôt qu'une nouvelle part corrective ou rien du tout. Mesuré par le planner le 2026-09-15 sur les fixtures `tracked_changes` et `combined` : le compte de marques est inchangé après chacun des deux outils, donc accepter toutes les révisions laisse le document porteur de changements de formatage que Word affichera encore comme révisions en attente. Ce n'est pas une régression — J02-P6 refuse explicitement les genres de révision non gérés plutôt que de les ignorer — mais un trou de périmètre qu'aucune part ni décision du plan n'a couvert ; `docs/audit/destructive-ops.md`, réécrit par J06-P5, le note et le date en notes complémentaires. Le plan atteint son objectif sans cela, et les deux genres demandent leur propre travail de fidélité (un `rPrChange` porte l'ancien `rPr` complet, à jeter en acceptation et à restaurer en rejet), donc une part sans revue derrière elle — J06-P4 est close et dernière du plan — serait mal couverte (options: ligne LONG_TERM_RECO — nouvelle part corrective étendant `accept`/`reject` aux deux genres — ne rien faire, le refus typé de J02-P6 valant avertissement ; reco: ligne LONG_TERM_RECO, l'écart mérite d'être écrit plutôt que découvert, tranché par le user en séance); plan updated: aucun (R-012 ajoutée à LONG_TERM_RECO.md)
- 2026-09-15 D-037 (user) Résolution de R-005 (nom) et du volet versions de R-009: publier sous un nom de fork propre plutôt que proposer J01–J06 en PR à l'amont ou renoncer à publier — la distribution et le dépôt GitHub deviennent `word-mcp-semantic` (dépôt renommé, remote et dossier local suivis), les quatre déclarations de version passent à **2.0.0** (`pyproject.toml` portait 1.6.20, la dernière release PyPI de l'amont héritée par le fork et jamais la nôtre ; `manifest.json`/`server.json` 1.3.0 ; `__version__` 1.2.0), et `tests/test_version_sync.py` garde nom et version contre une nouvelle dérive, y compris l'`identifier` de `server.json` et le trailer `mcp-name` du README. Attribution conservée là où elle est due : les deux copyrights antérieurs du LICENSE plus le nouveau, la section Acknowledgments, la note de fork du README, et les liens de changelog 1.0.0→1.5.1 qui pointent vers les releases de l'amont. **Publication PyPI non faite** : le user la remet à plus tard, `publish.yml` est donc en `workflow_dispatch` seul et documenté pour le Trusted Publishing (aucun token stocké). Le user a par ailleurs demandé une CI GitHub Actions, gratuite parce que le dépôt est public : matrice Linux/macOS/Windows × Python 3.11/3.12/3.13, job LibreOffice où les 16 tests marqués s'exécutent vraiment, job lint/docs/lock/build ; `scripts/check.sh` élargi pour rester le miroir local du job `checks`. **La CI a trouvé deux défauts réels invisibles depuis le Pi** (Linux, 3.12), corrigés dans la foulée : (1) le paquet était impossible à importer sous Python 3.11, que `requires-python` annonce pourtant — défaut de dataclass à `MappingProxyType` dans `engine/theme.py` et deux sites PEP 701 dans `core/word_mac.py`, le JXA produit étant prouvé inchangé ; (2) sous Windows, `scripts/gen_tools_md.py` lisait TOOLS.md et README.md avec le codec local (cp1252) et rapportait une fausse dérive, et `tests/support/test_libreoffice.py` reconstruisait un chemin depuis un `file://` URI par `removeprefix`, ce qui sur Windows donnait `/C:/...` — un site en échec franc et **deux assertions `not exists()` vraies par accident, incapables d'échouer**. Les `open()` sans encoding de `core/protection.py` et `core/unprotect.py` sont laissés tels quels : ils servent le sidecar `.protection` que R-004 propose de supprimer. Release `v2.0.0` créée, workflow `release.yml` vert, sdist et roue `py3-none-any` attachées — le paquet est pur Python, il n'existe pas d'artefact par plateforme (options: nom de fork — PR à l'amont — ne pas publier ; reco: nom de fork, tranché par le user en séance)
