<!-- one decision, at most 100 lines; keep exactly one of the two states below.
     The queue is drained by the closing gate of each session; what is left here is what
     the next session's startup gate has to ask. -->

# DECISION — v2-semantic-engine

No pending decision.

## Queue

- D-033 — `docs/audit/destructive-ops.md` est périmé : il décrit comme cassés 8 outils/comportements tous corrigés depuis, et dont plus aucun n'est `xfail` dans `tests/characterization/test_existing_tools.py` (`format_text`, `add_table_of_contents`, `merge_documents`, `add_header_footer`, `create_custom_style`, `replace_block_between_manual_anchors`, `track_replace`, la fusion de marque de paragraphe d'`accept`/`reject_tracked_changes`). Document produit par J01-P5 comme inventaire de départ ; il n'est dans les `files` d'aucune part et J06-P3 l'a laissé intact comme demandé. Options : nouvelle part T5 qui le réécrit en « état avant/après » — le retirer du dépôt, l'inventaire vivant étant désormais `tests/characterization` — le laisser tel quel avec un avertissement en tête. Reco : **réécrire en avant/après** (part T5, `depends_on: [J06-P4]`), c'est la seule trace de ce que le plan a réparé et elle vaut pour le CHANGELOG comme pour la revue ; le retirer perdrait l'historique, le laisser faux ferait retomber une session future dans le piège. Bloque : rien. Source : J06-P3 (worker).
- D-034 — Les docstrings de `engine/audit.py:1147` et `tools/v2/audit.py:44` annoncent « twelve checks … in the order of `FINDING_KINDS` » alors que `FINDING_KINDS` compte **15** entrées (vérifié par exécution le 2026-09-15) : les 12 contrôles du Scope se subdivisent en 15 genres (signets et plages de commentaire donnent chacun un genre `without_start` et un `without_end`). J06-P3 a évité de citer un compte dans le README pour ne pas propager l'écart. Options : corriger les deux docstrings en « fifteen finding kinds across twelve checks » — laisser la revue J06-P4 le traiter comme finding — ne rien faire. Reco : **corriger les deux docstrings**, c'est une ligne et l'écart est un signal faux pour qui ajoute un genre. Bloque : rien. Source : J06-P3 (worker), confirmé par exécution.
- D-035 — Les 3 should-fix + 1 optional de la revue J06-P4, tous reproduits par exécution, aucun bloquant. (1) `tools/v2/compare.py:176` — `engine_snapshot` lit le zip avant tout `DocxPackage.open` (`:180`) et `zipfile.BadZipFile` n'est ni dans `_REPORTED` ni une `OSError` : un `.docx` tronqué ou renommé fait **lever** `BadZipFile` hors de l'outil, là où `doc_inspect` et `doc_audit` répondent `{"status":"error","code":"package_error"}` — l'agent appelant ne peut pas brancher sur `code`, et le docstring de `tools/v2/registry.py` promet le contraire. (2) `tests/e2e/test_agent_flows.py:195` — la garde nomme `word/theme1.xml`, qui n'est pas une partie du paquet (le nom réel est `word/theme/theme1.xml`) : ce tiers de l'assertion est inerte et une réécriture du thème à la sauvegarde — exactement la classe de régression D-007 — la traverserait sans la faire échouer. (3) `engine/audit.py:931` — `_referenced_styles` ne balaie que `pkg.stories()`, commentaires exclus : un style personnalisé utilisé seulement dans `word/comments.xml` est rapporté `unused_custom_style`, et un agent qui agit sur le finding le supprime. (4) optional, `jalons/J02-coeur-ooxml.md:170` — la garde grep de D-007 ne balaie que `tests/` alors que `Diff` et un porteur (`delta.is_empty()`, `tools/v2/compare.py:184`) vivent désormais sous `word_document_server/`. Options : une part corrective T4 couvrant les 4 — les 3 should-fix seulement — les deux points de fidélité (2 et 4) seulement, qui touchent l'instrument. Reco : **part corrective T4 couvrant les 4**, le plan est clos par ailleurs, les quatre correctifs tiennent en quelques lignes chacun, et (2) comme (4) rendent le harnais sourd à une régression que tout le plan s'est employé à empêcher. Bloque : rien. Source : revue J06-P4.
