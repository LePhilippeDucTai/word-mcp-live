# PLAN — v2-semantic-engine

Goal: Faire de word-mcp-live un moteur de manipulation sémantique de documents Word pour agents IA : un cœur OOXML pur (Linux, sans Word) qui garantit la non-dégradation (styles, relations, champs, signets, commentaires, révisions, numérotation, mise en page), les outils existants migrés dessus sans rupture d'API, une surface V2 `doc_*` (adressage sans état, dry-run, styles/thème, audit, comparaison), les outils live COM/JXA préservés.
Base branch: main · Remote: origin · Language: fr · Created: 2026-09-09 · Planned with: best/max
Base note (2026-09-15, D-026) : les sessions s1–s8 ont travaillé sur `v2-semantic-engine`, fusionnée dans `main` à la clôture de s8 (`git rev-list --left-right --count main...v2-semantic-engine` → `0 94`, sans divergence) ; `main` est la base à partir de s9.

## Scope

- In: paquet `word_document_server/engine/` ; harnais de tests (fixtures générées python-docx+lxml et LibreOffice, instantanés canoniques, validateur de paquet, caractérisation) ; migration des outils cross-platform texte, révisions, commentaires, hyperliens, formatage, TOC, fusion, en-têtes ; retrait du monkey-patch de sauvegarde ; écritures atomiques ; outils `doc_*` (inspect, find, edit_text, format_range, apply_edits, capabilities, styles, effective format, list, table style, audit, compare) ; TOOLS.md et compteurs README générés ; README réécrit sur le MCP réel (D-025) ; garde-fous de dérive ; trois plantages macOS prouvés par lecture.
- Out: tout autre changement des modules COM/JXA ; outils de notes de bas de page (conservés tels quels) ; protection, signature, sidecar `.protection` ; création de styles de tableau ; normalisation automatique ; publication PyPI et versionnage ; CI hébergée (validation locale uniquement).

## Architecture

- `engine/` : Python synchrone, lxml sur l'OPC de python-docx (`Document.part`, `Part`, `relate_to`) ; aucune dépendance MCP ni Word ; exceptions typées (`PackageError`, `LocatorError`, `UnsupportedRange`, `InvalidText`, `UnsupportedRevision`).
- Couches : `package` (parties, stories, ids d'annotation, sauvegarde atomique temp+`os.replace`) → `textmodel` (flux de texte d'un paragraphe : segments texte, opaques, marqueurs, cachés ; champs complexes atomiques) → `ranges` (split, delete, insert, replace) → `format` (rPr en ordre de schéma, enveloppes hyperlink/commentaire) → `find`, `revisions`, `locators`, `inspect`, `styles`/`theme`/`effective`, `numbering`, `table_styles`, `merge`, `audit`, `compare`.
- Politique de texte : visible = `w:t` sous `w:r`, `w:hyperlink`, `w:ins`, `w:sdt`, `w:smartTag`, `w:fldSimple` ; `w:tab`→`\t`, `w:br`/`w:cr`→`\n` ; `w:del` et `w:instrText` cachés ; `drawing`, `footnoteReference`, `fldChar` opaques ; signets et plages de commentaire = marqueurs jamais supprimés.
- Outils existants : noms, paramètres et formats de retour inchangés ; leurs implémentations délèguent au moteur (J03). `main.py` n'est touché que pour enregistrer `tools/v2` et corriger le registre.
- Outils V2 : `tools/v2/*.py` découverts dynamiquement, schéma dérivé de la signature, retour dict `{status, dry_run, changes, warnings}` ou `{status: "error", code, message}`.
- Adressage V2 : locators sans état — `{"paragraph": i, "expect_text"}` (i = `w:p` en ordre du corps, sdt de bloc inclus, cellules et zones de texte exclues, base 0), `{"find", "occurrence", "within"}`, `{"bookmark"}`, `{"heading"}`, `{"table", "row", "col", "paragraph"}`, `{"story"}` ; aucun identifiant de session.
- Pas d'abstraction backend : COM/JXA restent une famille d'outils distincte ; `tools/platforms.py` porte la disponibilité par plateforme et alimente `TOOLS.md`, README et `doc_capabilities`.
- Tests : fixtures générées et déterministes (jamais de binaires commités) ; instantané = c14n par partie + signature des runs par paragraphe ; LibreOffice = producteur de fixtures et validateur d'ouverture optionnel, jamais source de vérité.

## Risks

- Invariants moteur : texte et rPr hors plage inchangés ; marqueurs conservés ; champs atomiques ; `w:del` jamais modifié ; parties non ciblées canoniquement identiques (faux à ce jour, D-007 ci-dessous ; rétabli par J02-P8), parties binaires identiques octet à octet ; ids d'annotation uniques ; enfants pPr/rPr/tblPr en ordre de schéma ; sauvegarde atomique ; LibreOffice rouvre le fichier ; index V2 sans les cellules ni les zones de texte ; un run ne quitte jamais son `w:hyperlink`/`w:sdt`/`w:smartTag` (ces deux derniers faux jusqu'à J02-P10, D-009 ci-dessous).
- Zones sensibles : `core/tracked_changes.py` (boucle infinie, splice multi-parents), `format_text`, `add_table_of_contents`, `merge_documents`, `add_header_footer`, `utils/save_utils.py`, numérotation (`numId` 1/2 en dur).
- Compatibilité : 120 noms d'outils, signatures et formats de retour inchangés ; live COM/JXA intact hors J04-P4 ; nouveaux paramètres optionnels seulement. L'invariant porte sur les noms, paramètres et formats de retour, pas sur trois comportements que J03 a changés (D-015) : `merge_documents` fusionne dans le premier document source au lieu d'un `Document()` vierge et refuse une source à commentaires ou notes ; `replace_block_between_manual_anchors` signale une ancre de fin introuvable au lieu de supprimer jusqu'à la fin du document ; les deux outils de blocs refusent un remplacement qui perdrait un `w:sectPr`. J03-P11 les écrit dans `CHANGELOG.md` (entrée `[Unreleased]`) ; J03-P8 relit l'entrée contre le code. J04-P7 en ajoute trois (D-016, D-018, D-019), le premier porté à la 5e puce `[Unreleased]` (D-017) : `paragraph_index` de `get_paragraph_text_from_document` et `set_paragraph_spacing` et `target_paragraph_index` des trois `insert_*_near_text` comptent désormais les paragraphes d'un `w:sdt` de bloc, comme `find_text_in_document`, `delete_paragraph` et `add_bookmark` depuis J03 — un index enregistré avant le changement peut désigner un autre paragraphe dès qu'un sdt de bloc précède ; `add_bookmark` refuse un index négatif au lieu de signer le dernier paragraphe ; `search_and_replace` rend `… found. N skipped (inside fields).` au lieu de `… found., N skipped (inside fields)` (suffixe né en J03-P1, décrit nulle part) ; J04-P6 relit les trois contre le code. J05 en ajoute sept (D-027), portés par J05-P8 à `[Unreleased]` en cinq puces et relus par J06-P4 : `insert_numbered_list_near_text` alloue une définition de liste neuve à chaque appel (nouveaux `abstractNum` et `num` dans `numbering.xml`, créé s'il manque) au lieu de rejoindre `numId` 1 ou 2 — deux appels successifs ne numérotent plus en continu, aucun `numId` existant n'est modifié — et n'applique plus `Normal` en repli quand `List Paragraph` manque ; `insert_line_or_paragraph_near_text` termine sa réponse par `with style 'Normal'.` au lieu du `repr` d'un `_ParagraphStyle` (D-021) ; `core.styles.ensure_heading_style` et `core.styles.create_style` écrivent réellement au lieu d'être des no-op, `create_style` rend un `StyleInfo` au lieu d'un `Style` python-docx ; `create_custom_style` écrit vraiment le style et refuse une couleur illisible au lieu de l'ignorer ; `add_heading` et `create_document` peuvent écrire `word/styles.xml` (jusqu'à 9 styles de titre) ; sur une feuille sans `Normal`, `add_heading` échoue jusqu'à J05-P9 puis crée les titres manquants sans `basedOn`/`next` (D-032, bullet ci-dessous).
- Portabilité : LibreOffice optionnel (`skip`), jamais requis par les tests unitaires ; aucun test ne dépend de Word ; `uv` hors PATH des shells (préfixer `PATH="$HOME/.local/bin:$PATH"`).
- Piège d'instrument (D-013) : `soffice` s'attache à toute instance déjà lancée sur le même profil (`~/.config/libreoffice` par défaut) et sort alors en code 0 **sans convertir** — mesuré en s4 sur trois `tests/characterization` en parallèle (`RuntimeError: soffice reported success but did not produce …`, victime différente à chaque relance, aucune régression de code). J03-P10 donne à chaque appel de `convert()` (`tests/support/libreoffice.py`) un profil jetable par `-env:UserInstallation=file://…`, gardé par le test à faux `soffice` de `tests/support/test_libreoffice.py` ; ne jamais lancer `soffice` sans ce profil. Tant que J03-P10 n'est pas fusionnée, un rouge de ce type sur J03-P5 se relance, il ne se corrige pas dans le code ; LibreOffice reste `skip` s'il manque.
- Espaces d'index : trois espaces de `paragraph_index` coexistaient (python-docx corps, `//w:p`, `body//w:p`) ; J03 puis J04-P7 (D-016) alignent sur l'espace V2 sans renommer les paramètres. Dans l'espace V2 (D-006, étendu par D-009), un paragraphe en cellule de tableau ou en zone de texte (`w:txbxContent`, `mc:AlternateContent`) n'a pas d'index : `Match.index` et `Revision.paragraph_index` valent `None`, seul un locator `table` atteint la cellule et seul `find` atteint la zone de texte ; `_v2_index_map` (`engine/find.py`) est l'unique filtre de cet espace, partagé par `find` et `list_revisions` — J04-P1 le réutilise, ne le réécrit pas ; côté outils, `indexed_paragraphs()` (`utils/document_utils.py`, J03-P5) l'enveloppe et est l'unique point d'entrée d'un `paragraph_index` public — `find_text_in_document`, `delete_paragraph`, `add_bookmark` (J03), `get_paragraph_text_from_document`, `set_paragraph_spacing` et le `target_paragraph_index` des trois `insert_*_near_text` (J04-P7) — jamais un second filtre. Écart assumé (D-016) : les 5 outils de notes de `tools/footnote_tools.py` (`add_footnote_to_document`, `add_endnote_to_document`, `add_footnote_enhanced`, `add_footnote_robust_tool`, `add_footnote_to_document_robust`) lisent toujours `paragraph_index` dans `doc.paragraphs` (enfants directs du corps, `w:sdt` de bloc exclu) — hors périmètre du plan (Scope « Out »), unifiés par R-003 ; `find_text_in_document("X") → i` puis `add_footnote_to_document(i, …)` annote donc un autre paragraphe dès qu'un `w:sdt` de bloc précède : ce n'est pas un bug à redécouvrir, aucune revue ne le compte comme finding ; `"body"` n'est qu'un alias d'entrée de `"document"` dans le filtre `stories` de `find`, jamais un id rendu.
- Correctifs D-009 (J02-P10), faux jusque-là et prouvés par sonde en J02-P7 sans qu'aucune des 19 fixtures ne les couvre : (1) un paragraphe de zone de texte ne consomme pas d'index V2 — avant J02-P10 il décalait tous les index suivants (`[p0 portant une zone, p1]` → `p1.index == 2`), soit un contrat `{"paragraph": i}` faux pour tout document à zone de texte ; (2) `tracked_insert` ne fait jamais sortir un run de son `w:hyperlink`/`w:sdt`/`w:smartTag` — avant J02-P10, insérer dans le `w:ins` d'un autre auteur enveloppant un conteneur scindait le conteneur et le texte qui suivait perdait son lien ; désormais l'insertion englobante est repoussée dans le conteneur (`w:hyperlink/w:ins`, forme de Word), jamais le conteneur cloné ni scindé ; écart assumé : un conteneur dont toutes les révisions sont rejetées reste, vide. Les fixtures `text_boxes` et `tracked_containers` (19 → 21) couvrent les deux formes ; J02-P7 (close) ne relit pas J02-P10 : J03-P8 s'en charge.
- Champs imbriqués (D-005) : le résultat en cache d'un champ imbriqué (un `PAGE` dans l'instruction d'un `IF`) est stocké en `w:t` ordinaire, donc lu comme texte visible par `textmodel` — `fields()` peut rendre `"1first page"` là où Word n'afficherait que `"first page"`, `visible_text` et `find` le voient de même. `fields()` signale les deux spans : `ranges` refuse d'y couper, et une correspondance de `find` qui y tombe est ignorée et signalée par J03-P1, jamais corrigée. Écart assumé, aucun code à changer ; J03-P1 et J06-P2 ne doivent pas le redécouvrir comme un bug (pour `compare`, un cache de champ qui change entre deux versions est un changement de texte, attendu).
- Zones aveugles du harnais laissées telles quelles (D-004) : `TableSignature` ne retient du `w:tblGrid` que le nombre de `w:gridCol` — des largeurs `w:gridCol/@w:w` réécrites passent `paragraphs=[...]` (mesuré sur `combined` ; `w:tcW` reste couvert par `cell_properties`) ; `XML-MALFORMED` et `CT-PART-MISSING` de `validate_package` sont fonctionnels mais sans test positif. Un test qui touche la grille d'un tableau (J03-P4, J05-P5) ne peut pas s'en remettre à `assert_unchanged_except` pour elle.
- Aller-retour `open`/`save` non neutre (D-007) : mesuré le 2026-09-09 sur 19/19 fixtures, `_EnginePartFactory` (J02-P1) fait parser par python-docx (`remove_blank_text=True`) des parties qu'il laissait en blob — `word/theme/theme1.xml`, `word/webSettings.xml`, `word/fontTable.xml`, `word/stylesWithEffects.xml`, `docProps/app.xml`, `customXml/itemProps1.xml` — resérialisées sans leurs blancs inter-éléments ; données intactes, invariant faux. J02-P8 restreint la fabrique à `LIVE_CONTENT_TYPES` (stories, famille des commentaires, styles, numbering) ; toute autre partie XML reste un blob, lisible par `Part.blob` seulement (J05-P1 pour le thème). Ne jamais tolérer ces parties en `parts=` d'`assert_unchanged_except` : le harnais deviendrait sourd à leur réécriture pour J03→J06.
- Piège d'instrument (D-007) : `Diff.is_empty` est une **méthode** — `assert delta.is_empty` est toujours vrai et a masqué le défaut ci-dessus sur 19 fixtures ; écrire `is_empty()` partout. `Diff` vit depuis J06-P2 dans `word_document_server/engine/compare.py` (`tests/support/snapshot.py` n'est plus qu'un réexport) et son porteur de production est `delta.is_empty()` dans `tools/v2/compare.py` ; la garde grep posée par J02-P8 sur les porteurs (`difference`, `delta`, `diff(...)`) ne balayait que `tests/` — J06-P5 (D-035) la porte sur les deux arbres, `tests/` et `word_document_server/`, en acceptation ; l'acceptation de J02-P8 reste telle quelle. `Pieces.is_empty` (`engine/ranges.py`) est, elle, une propriété.
- Contrat d'enveloppe des outils `doc_*` (D-035, corrigé par J06-P5) : toute erreur d'ouverture de paquet ressort en `{"status": "error", "code": "package_error"}`, jamais en exception — `tools/v2/registry.py` ne rapporte que `_REPORTED` (`EngineError`, `OSError`, `ValueError`, `TypeError`) et laisse tout le reste remonter comme bug du serveur ; seul `DocxPackage.open` traduit une archive illisible en `PackageError`. Le trou était `zipfile.BadZipFile` : `doc_compare` faisait lire le zip par `engine_snapshot` avant tout `DocxPackage.open`, donc un `.docx` tronqué ou renommé levait hors de l'outil là où `doc_inspect`/`doc_audit` répondent `package_error` (reproduit le 2026-09-15). Règle : un outil `doc_*` qui lit un fichier autrement que par `DocxPackage.open` l'ouvre d'abord par `DocxPackage.open`, et son test d'enveloppe passe par `v2_tools()` sur un non-zip (modèle `test_a_file_that_is_not_a_package_is_reported`, `tests/tools/test_v2_text.py`). Même revue : `_referenced_styles` (`engine/audit.py`) ignorait `word/comments.xml` — un style appliqué seulement dans un commentaire était rapporté `unused_custom_style` ; tout balayage « qui référence quoi » couvre `pkg.stories()` **et** la racine des commentaires (`_comments_root`). Et une garde de parties « non touchées » (`tests/e2e/test_agent_flows.py`) nommait `word/theme1.xml`, qui n'existe pas (`word/theme/theme1.xml`) : un nom de partie dans une assertion se vérifie contre le paquet, sinon l'assertion est inerte.
- Inventaire de départ (D-033, réécrit par J06-P5) : `docs/audit/destructive-ops.md` décrit l'état **avant** le plan (photo J01-P5 du 2026-09-09 : huit outils/comportements cassés, tous corrigés par J03-P2/P4/P5/P6 et J05-P3) puis l'état après, avec le test de caractérisation qui gèle chaque correctif ; l'inventaire vivant est `tests/characterization/test_existing_tools.py`, sans plus aucun `xfail`. Ne pas y relire un outil comme cassé ; ne pas le retirer (seule trace de l'état initial). Les docstrings d'`audit()` et de `doc_audit` comptent en genres, pas en contrôles : `len(FINDING_KINDS)` = 15 sur douze contrôles (D-034) — un genre ajouté se reporte dans la constante, les deux docstrings et `tests/engine/test_audit.py`.
- Lint partiel : `[tool.ruff] include` ne couvre que `word_document_server/engine/**`, `tests/**`, `scripts/**` ; un `ruff check .` vert ne dit rien de `word_document_server/tools|core|utils` (95 RUF013 et 9 W605 sur `live_tools.py` seul), que J03 réécrit.
- Piège d'instrument (D-024) : `discover_tool_specs()` (`tools/v2/registry.py`) ignore en silence tout module de `tools/v2/` sans liste `TOOLS`, et `TOOLS.md`, les compteurs README et `test_registry_consistency` dérivent du registre — `doc_capabilities` (J04-P3) est resté non enregistré, invisible de `gen_tools_md.py --check` comme des tests, jusqu'à la revue J04-P6. J05-P0 l'enregistre et pose `test_every_v2_module_exports_tools` ; chaque module `tools/v2/*.py` ajouté par J05-P1..P5 et J06 exporte `TOOLS` et entre ses noms dans `_V2_TOOLS` (`tools/platforms.py`), la revue le vérifie par `mcp.list_tools()`. Même revue : `doc_apply_edits` ne validait pas les clés de ses charges (une faute de clé effaçait la plage visée et sauvegardait en `status: ok`) — les listes blanches de J05-P0 sont l'unique contrôle, alignées sur les signatures de `doc_edit_text`/`doc_format_range` ; un paramètre ajouté à l'un se reporte dans l'autre.
- Piège de génération (D-025) : `README.md` est en partie généré — le bloc entre `<!-- tool-counts:start -->` et `<!-- tool-counts:end -->` est écrit par `scripts/gen_tools_md.py` (`apply_readme_counts`, `:187-195`) et le fichier se termine par le trailer `<!-- mcp-name: io.github.ykarapazar/word-mcp-live -->` (registre MCP, `server.json`). Une réécriture qui perd un marqueur fait échouer `gen_tools_md.py --check` (exit 1, « README.md is missing … markers ») et `tests/test_docs_sync.py` ; le bloc ne s'édite jamais à la main. Toute part qui touche le README (J05-P0, J05-P7, J06-P3) écrit autour des marqueurs et garde le trailer en dernière ligne ; les compteurs suivent le registre, donc un module `tools/v2` fusionné sans régénération rend `--check` rouge sur la base — se corrige par régénération sur la base (comme après J04-P5), jamais dans une part qui n'a pas `TOOLS.md` dans ses `files`.
- Régression J05-P3 (D-032, corrigée par J05-P9) : `ensure_heading_style` (`core/styles.py:106`) ne rattrape que `already_exists` ; sur un document dont la feuille ne définit pas `Normal` et à qui manque un `HeadingN`, `_reference_id` lève `LocatorError("not_found")` et `add_heading` répond « Failed to add heading: 'based_on' names 'Normal', which this document does not define… » — reproduit le 2026-09-15 sur `simple` privé de `Normal` et `Heading7-9`, niveau 1 refusé alors que `Heading1` existe — là où le code d'avant J05 (`except Exception: pass`) insérait le titre ; un paquet dont la partie styles n'est pas du XML vivant échoue de même par `PackageError`. « Ensure » est au mieux-effort par contrat : tant que J05-P9 n'est pas ✅, un rouge de cette forme ne se redécouvre pas et ne se contourne pas dans une autre part. Même revue, optional : `_OFF` recopié dans `engine/effective.py:194` depuis `engine/styles.py:246` — jeux de clés disjoints, aucune incohérence, mais un vocabulaire élargi d'un seul côté ferait diverger les six bascules extras des cinq du modèle ; J05-P9 importe `_toggle_value` au lieu de la constante.

## Checks

- Setup: `PATH="$HOME/.local/bin:$PATH" uv sync` · marker: `.venv/`
- Test: `PATH="$HOME/.local/bin:$PATH" uv run pytest tests/ -q` · Build: `PATH="$HOME/.local/bin:$PATH" uv build` · Lint: `PATH="$HOME/.local/bin:$PATH" uv run ruff check .`
- Baseline 2026-09-09: green — test : 1 passed (3 s, rejoué à la révision D-002) ; lint et build utilisables à partir de J01-P1 (ruff absent avant).
- Lock: `PATH="$HOME/.local/bin:$PATH" uv lock --check` → 0 ; `uv.lock` régénéré le 2026-09-09 (périmé depuis v1.1.3, il se réécrivait à chaque `uv run`) ; un `uv run` ne doit plus modifier l'arbre.

## Milestones

| Id  | Milestone | Parts | Depends on | Orchestrator | File |
|-----|-----------|-------|------------|--------------|------|
| J01 | Harnais de fidélité documentaire | 7 | — | opus/high | jalons/J01-harnais-fidelite.md |
| J02 | Cœur OOXML : paquet, flux de texte, plages, révisions | 10 | J01 | opus/high | jalons/J02-coeur-ooxml.md |
| J03 | Migration des outils existants sur le cœur | 11 | J02 | opus/high | jalons/J03-migration-outils.md |
| J04 | Surface V2 : adressage, inspection, dry-run, capacités, docs | 7 | J03 | opus/high | jalons/J04-surface-v2.md |
| J05 | Styles, thème, format effectif, numérotation, styles de tableau | 10 | J04 | opus/high | jalons/J05-styles-theme-numerotation.md |
| J06 | Audit, comparaison et validation de bout en bout | 5 | J05 | opus/high | jalons/J06-audit-validation.md |

## Tiers

| Tier | Model  | Effort | Use for | Executor |
|------|--------|--------|---------|----------|
| T1 | best   | max    | planning, architecture, cross-cutting decisions, plan updates | orchestrator inline; philippele-skills:planner |
| T2 | opus   | xhigh  | complex refactor, tricky logic, concurrency, migrations, milestone review | philippele-skills:worker-t2 / reviewer |
| T3 | opus   | high   | standard implementation with design latitude | philippele-skills:worker-t3 |
| T4 | sonnet | high   | well-specified implementation, tests | philippele-skills:worker-t4 |
| T5 | sonnet | medium | mechanical changes, docs, renames, config | philippele-skills:worker-t5 |
| T6 | haiku  | low    | trivial edits, search, inventory | philippele-skills:worker-t6 |

## Conventions

- Writers: orchestrator → PROGRESS.md, DECISION.md, DECISIONS_LOG.md, LONG_TERM_RECO.md; planner → PLAN.md, jalons/; workers → source only.
- Every milestone ends with a `kind: review` part run by philippele-skills:reviewer; the reviewer also runs the Checks and reads Risks.
- Wave cap: 4 parts in flight; review, size L, and T1 parts run alone.
- Git: workers commit in their worktree; one `--no-ff` merge commit per validated part `<id>: <title>` onto `main`; push `main` to origin at session end (none when Remote is none); never force, never amend. Sessions s1–s8 merged onto and pushed `v2-semantic-engine`, merged into `main` at the end of s8 (D-026).
- Worktrees: `isolation: "worktree"` of the harness branches from the repository's default branch (`main`, the base since s9) **as snapshotted when the session started**, not from its current tip — measured in s9: `main` at `29e9ebf` at start, `24b7a4b` after J05-P9's merge, and the worktree made for J05-P8 still branched at `29e9ebf`. It is safe only for a part whose `depends_on` (and the files it edits) were all merged before the session began. Any part depending on a part merged in the **same** session gets a hand-made worktree (`git worktree add -b worktree-<id> .claude/worktrees/<id> main`) and is dispatched **without** `isolation`. This succeeds the D-003 exception of s1–s8 (hand-made worktrees on `v2-semantic-engine`, because `isolation: "worktree"` ignored `worktree.baseRef` and branched from a `main` that lacked the plan's commits): D-026 moved the base to `main`, it did not end the need; the trace stays in DECISIONS_LOG.md.
- Failed part: try 2 at the same tier with the failure evidence, try 3 one tier higher, then ❌ and a queued decision; a `blocked` report or a first merge conflict does not count.
- Plan updates: only when remaining parts are invalidated or the user asks for a change; planner rewrites files in place; ids never renumbered; a dropped part is ⏭ and its dependents get their Scope rewritten.
- Code, docstrings, descriptions d'outils, TOOLS.md et CHANGELOG en anglais ; documents de plan en français. Aucun workflow GitHub Actions n'est ajouté.
